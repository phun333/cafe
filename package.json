{
  "name": "cafe.bayburt.lu",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "content": "node scripts/build-content.js",
    "dev": "pnpm content && vite",
    "build": "pnpm content && tsc && vite build",
    "preview": "vite preview",
    "start": "npx serve dist -s -l 3000"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^17.0.1"
  }
}